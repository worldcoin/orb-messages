[package]
name = "orb-mcu-messaging"
version = "0.3.0"
authors = [
    "Cyril Fougeray <cyril.fougeray@toolsforhumanity.com>",
]
edition = "2021"
rust-version = "1.74"
license = "MIT OR (Apache-2.0 WITH LLVM-exception)"

[features]
default = []
# Enables the private code. Requires providing the path to the private .proto
# files via the `ORB_MCU_MESSAGING_PRIVATE_PATH` env var at build time.
private = []

[dependencies]
prost = "0.12.3"

[build-dependencies]
prost-build = "0.12.3"
